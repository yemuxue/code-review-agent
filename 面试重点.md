7. Execution Loop 怎么实现的？关键代码是什么？


8. Streaming Parser 怎么处理流式 tool call？


状态机逐块拼接
```python
class ParserState(Enum):
    """流式解析器状态：LLM 返回的内容是逐块到达的，需要状态机拼装"""
    IDLE = 0                # 初始状态，等待 chunk 到达
    COLLECTING_TEXT = 1     # 正在收文本片段（LLM 在打字回复）
    COLLECTING_TOOL_NAME = 2  # 正在收工具名
    COLLECTING_ARGS = 3     # 正在收 JSON 参数片段（可能分 20 块才拼完）

class StreamingParser:
    def __init__(self):
        self.state = ParserState.IDLE
        self.tool_name = ""   # 累积的工具名
        self.tool_args = ""   # 累积的参数字符串（JSON 片段）
        self.text = ""        # 累积的文本内容

    def feed(self, chunk: Chunk) -> list[Event]:
        """
        每次收到一块数据就喂给解析器。
        返回解析出的 Event 列表（可能 0 个或多个）。
        """
        events = []

        # ── 情况 1：内容块有增量更新 ──
        if chunk.type == "content_block_delta":
            if chunk.delta.type == "text_delta":
                # LLM 在输出文字（比如 "我正在分析..."）
                self.state = ParserState.COLLECTING_TEXT
                self.text += chunk.delta.text
                events.append(Event("text", chunk.delta.text))  # ← 前端逐字渲染

            elif chunk.delta.type == "input_json_delta":
                # LLM 在输出 tool call 的 JSON 参数（分块到达）
                # 例如 chunks = ['{"file_path": "', 'agent.py"}']
                self.state = ParserState.COLLECTING_ARGS
                self.tool_args += chunk.delta.partial_json  # ← 累加 JSON 片段
                events.append(Event("tool_args", chunk.delta.partial_json))

        # ── 情况 2：内容块结束了 → JSON 应该完整了 ──
        elif chunk.type == "content_block_stop":
            if self.tool_name:
                # 拼接完整的 JSON 参数
                args = json.loads(self.tool_args)  # ← 将 '{"file_path":"agent.py"}' 解析成 dict
                events.append(Event("tool_call", {"name": self.tool_name, "args": args}))
                # 重置状态，准备处理下一个 tool call
                self.tool_name = ""
                self.tool_args = ""
            self.state = ParserState.IDLE  # 回到空闲

        return events
```



12. Sandbox 进程隔离怎么实现的？
**实现**（'sandbox.py'）：


为什么要用线程？
主进程                        线程
  │                             │
  │── t.start() ────────────────→ subprocess.run(["buggy_command"])
  │                             │     ↓
  │── t.join(timeout=35) ───────→ 子进程卡死了...
  │                             │
  │ (等 35 秒)                  │
  │                             │
  │ t 还没结束 → timed_out=True  │  ← 线程还在卡，但主进程已经判超时了
  │ 返回结果                     │
  ▼                             ▼
继续执行                      (悬空，最终被 Python GC 回收)


防线 ①：shlex.split() — 把字符串拆成列表
防线 ②：shell=False（subprocess 默认）— 不经过 shell 解析
防线 ③：HITL — 即使注入成功也会被拦


完整执行流程示例
User: "运行 pylint 检查代码"
    │
    ▼
Agent turn 1: LLM → "我需要跑个命令检查代码质量"
    → tool_call: run_command(command="pylint src/harness/agent.py --output-format=text")
    │
    ▼
_execute_single_tool(tc):
    │
    ├── ① HITL 检查
    │   needs_approval("run_command", {"command": "pylint src/..."})
    │   → assess_risk: 工具名 "run_command" = MODERATE
    │   → 命令内容 "pylint" 不在危险关键字里 → MODERATE 确认
    │   → request_approval: MODERATE → 默认批准 ✅
    │
    ├── ② shlex 解析
    │   "pylint src/harness/agent.py --output-format=text"
    │   → ["pylint", "src/harness/agent.py", "--output-format=text"]
    │
    ├── ③ Sandbox.run()
    │   │
    │   │  target():
    │   │    subprocess.run(
    │   │      ["pylint", "src/harness/agent.py", "--output-format=text"],
    │   │      timeout=60,
    │   │      capture_output=True,
    │   │      cwd="/tmp/sandbox_abc123/",
    │   │      text=True
    │   │    )
    │   │
    │   │  线程执行...3秒后完成
    │   │  → exit_code=4, stdout="************* Module agent\nC: 45,0: Line too long...\n"
    │   │  → t.join(65) → 线程已结束
    │   │
    │   └── SandboxResult(exit_code=4, stdout="...", stderr="", timed_out=False)
    │
    ├── ④ summary() 格式化给 LLM
    │   = "[EXIT:4]\nSTDOUT:\n************* Module agent\nC: 45,0: Line too long...\n"
    │
    └── 结果塞回 messages
        → LLM 看到 pylint 输出后说："发现一个 line-too-long 问题..."

Turn 2: LLM → "agent.py 第 45 行太长，建议拆分"


### 26. 你在项目里用了 Prometheus 做什么？

**问题**：Prometheus 在这个项目里监控了哪些指标？


**自动采集指标**（通过 `prometheus-fastapi-instrumentator`）：


**面试话术**
"第一层是 API 层——请求数、延迟、错误率、并发。这一层由 prometheus-fastapi-instrumentator 自动捕获，零配置。"

"第二层是 Agent 层——每轮 token 消耗、工具调用次数、记忆压缩频率。这些需要手动埋点，在 _execute_single_tool() 和 chat() 里加计数器。"

"第三层是业务层——发现数量、精准率/召回率、F1。这一层跑 eval 数据集时算，可以用 gauge 暴露，在 Grafana 上画回归曲线。"


### 30. 这个项目和工业级系统的差距在哪里？怎么改进？

**问题**：你的项目距离生产环境还差什么？

**回答**：

| 差距 | 当前实现 | 工业级方案 | 改进优先级 |
|------|---------|-----------|-----------|
| **用户管理** | JSON 文件持久化 | PostgreSQL + 用户组 RBAC | ⭐⭐⭐ |
| **Token 管理** | 内存吊销列表 | Redis Set + TTL 自动过期 | ⭐⭐⭐ |
| **向量搜索** | SQLite FTS5（全文搜索） | ChromaDB/Pinecone 语义搜索 | ⭐⭐ |
| **容器化** | 单机 Docker | K8s + HPA 自动扩缩 | ⭐⭐⭐ |
| **监控告警** | Prometheus 指标 | Grafana 面板 + PagerDuty 告警 | ⭐⭐ |
| **日志** | JSON Lines 文件 | ELK/Loki 集中式日志 | ⭐⭐ |
| **API 版本** | 无版本控制 | /v1/ /v2/ 多版本并行 | ⭐ |
| **单元测试** | 12 个集成测试 | 80%+ 覆盖率 + 快照测试 | ⭐⭐⭐ |
| **缓存** | 单进程 LRU | Redis 分布式缓存 | ⭐⭐ |
| **并发** | 单线程处理 | 异步 + 队列 + Worker 池 | ⭐⭐⭐ |

**最小改进路径**：

```
第一步: PostgreSQL + Redis（核心短板）
   ↓
第二步: K8s 部署（可扩展性）
   ↓
第三步: 80% 测试覆盖（质量保障）
   ↓
第四步: ELK 日志 + Grafana 面板（可观测性）
```

**面试话术**：*"承认差距比掩饰更有价值。这个问题面试官想听的不是你的项目有多完美，而是你有没有思考过'如果用户量翻 100 倍怎么办？'——我的回答是先把用户存储和缓存换成 PostgreSQL + Redis，这是扩容的基础。"*

---

## 面试个人介绍

我从零构建了一个完整的代码审查 Agent 系统，核心是手写的 Agent Harness 运行时——包括 ReAct 执行循环、流式解析状态机、LRU+TTL 缓存、三级 HITL 审批和线程级沙箱隔离，没有依赖 LangChain 等框架。上层用 LangGraph 实现了 Plan → Execute → Review 三阶段 Multi-Agent 管线，三个 Agent 角色分离——Planner 激进扫描、Executor 严谨验证、Reviewer 客观输出——并通过条件路由在无发现时跳过后续阶段节省 token。同时构建了 33 条手工标注的 Eval 数据集，实测 Precision 100%、Recall 60%，形成"改进→评估→验证"闭环。基础设施方面集成了 JWT 双 Token 认证、Prometheus 指标采集、GitHub Actions 五道门 CI 和 Streamlit Web UI。

我的核心优势有三点：第一，**理解 Agent 底层原理而不是调包**——手写了 Execution Loop、Streaming Parser、Memory 压缩、HITL 审批、Sandbox 隔离等全部组件，面试时能解释每一行的设计意图。第二，**有评估方法论和改进闭环**——标注了 33 条样本的 Eval 数据集，跟踪 Precision/Recall/F1，通过调整 prompt 策略把精度从不到 30% 提升到 100% Precision、60% Recall。第三，**有生产化意识**——项目包含 JWT 双 Token 认证、Prometheus 监控、HITL 安全审批、CI/CD 五道门，不只是能跑的 Demo，考虑了上线后的运维和安全。

### 面试高频问题快速应答

**Q: 你做过什么 Agent 项目？**

> 我写了一个代码审查 Agent 系统，从底层引擎到前端 UI 全部自己实现。核心是手写的 Agent Harness——Execution Loop、Streaming Parser、Memory 压缩、HITL 审批、Sandbox 隔离——没有依赖 LangChain。上层用 LangGraph 做了 Plan → Execute → Review 三阶段 Multi-Agent 管线。

**Q: 最大技术难点是什么？**

> Agent 系统里多个组件要协同工作。LLM 返回的 tool call 可能是 JSON 也可能是 XML，需要兼容解析；流式输出分块到达，用状态机逐块拼装；Multi-Agent 管线里三个 Agent 各有各的 prompt，信息要在它们之间用结构化格式传递；最大的坑是 OpenAI 和 Anthropic 的参数格式不同——一个 arguments 是 JSON 字符串，一个 input 是 dict，不转换就直接报错。

**Q: 你怎么衡量 Agent 效果？**

> 我标注了 33 条样本做 Eval，跟踪 Precision/Recall/F1。通过改 prompt 策略——Planner 让它"宁可误报不可漏报"，Executor 让它"没证据就是 FALSE_POSITIVE"——最终达到 100% Precision、60% Recall。Agent 不是调参调出来的，是 prompt 工程和架构设计共同优化的结果。

