# Agent/LLM 应用开发实习 — 面试高频 30 题

> 基于项目 [code-review-agent](https://github.com/yemuxue/code-review-agent) 实战经验整理
> 覆盖 Agent 基础 → 多 Agent 编排 → 生产部署 → LLM 原理 全链路

---

## 目录

- [一、Agent 基础概念（1-5）](#一agent-基础概念1-5)
- [二、Agent Harness 实现（6-12）](#二agent-harness-实现6-12)
- [三、多 Agent 与编排（13-17）](#三多-agent-与编排13-17)
- [四、LLM 原理与 API（18-23）](#四llm-原理与-api18-23)
- [五、生产化与面试综合（24-30）](#五生产化与面试综合24-30)

---

## 一、Agent 基础概念（1-5）

### 1. 什么是 Agent？和传统程序有什么区别？

**问题**：请解释 LLM Agent 的概念，以及它和传统软件程序的核心区别。

**回答**：

LLM Agent 是一个**自主决策 + 工具调用 + 环境交互**的循环系统。核心公式：

```
Agent = LLM（大脑）+ Tools（手脚）+ Memory（记忆）+ Execution Loop（循环）
```

与传统程序的**核心区别**：

| 维度 | 传统程序 | LLM Agent |
|------|---------|-----------|
| 控制流 | if-else / switch 硬编码 | LLM 动态决策（ReAct 循环） |
| 输入 | 结构化参数 | 自然语言（模糊、开放） |
| 容错 | 崩溃就挂 | 可重试、可回退、可解释失败 |
| 工具使用 | 函数调用写死 | LLM 自主选择工具 + 参数 |
| 记忆 | 数据库/变量 | 对话历史 + 向量检索 + 摘要 |

**面试话术**：*"Agent 和传统程序最大的区别在于控制流是在运行时由 LLM 动态决定的，而不是编译时写死的。我的项目里用 Execution Loop 实现：LLM 收到任务 → 决定调什么工具 → 解析结果 → 继续推理，直到任务完成或达到最大轮次。"*

---

### 2. ReAct 模式是什么？怎么实现的？

**问题**：解释 ReAct（Reasoning + Acting）模式及其实现方式。

**回答**：

ReAct 是 **Reasoning（推理）+ Acting（行动）** 的交替循环，由 Shunyu Yao 在 2022 年提出。核心思想：LLM 先思考再行动，观察结果后再思考，形成闭环。

```
Thought:  我需要先看看项目里有什么文件
Action:    list_files(path="./src")
Observation: ["agent.py", "streaming.py", ...]
Thought:  有 agent.py，看看它的代码
Action:    read_file(file_path="./src/agent.py")
Observation: class AgentHarness: def run(): ...
Thought:  找到问题了！
Answer:   发现 agent.py 缺少异常处理...
```

**实现方式**（我项目中的 `agent.py`）：

```python
def run(self, task: str) -> str:
    # ① 把用户任务追加到对话历史
    # messages = [system_prompt, user_task, assistant_reply, tool_result, ...]
    self.messages.append({"role": "user", "content": task})

    for turn in range(self.max_turns):
        # ② 调 LLM：传入完整对话历史 + 工具定义
        # LLM 输出两种可能：
        #   A) tool_calls = [...]  → 还要调工具
        #   B) tool_calls = None   → 直接返回文本答案
        response = self.model.chat(messages=self.messages, tools=self.tools)

        if response.tool_calls:
            # ③ Acting: LLM 决定调工具，逐个执行
            for tc in response.tool_calls:
                result = self._execute_single_tool(tc)  # 执行工具（读文件/搜索/跑命令）
                # ④ Observation: 工具结果塞回对话
                # 这样 LLM 下一轮就知道"我刚才 ls 看到哪些文件了"
                self.messages.append({"role": "tool", ...})
        else:
            # ⑤ Answer: LLM 完成推理，返回最终答案
            self.messages.append({"role": "assistant", "content": response.content})
            return response.content

    # ⑥ 循环用完都没 return → 强制结束（防止死循环兜底）
    return self._force_finish()
```

**面试话术**：*"ReAct 让 Agent 的每一步决策都可解释——面试官能看到 LLM 为什么调这个工具、看到了什么结果、下一步怎么决定。这也是 Agent 调试的核心手段。"*

---

### 3. Agent 有哪些类型？你的项目用了什么模式？

**问题**：常见的 Agent 架构类型有哪些？

**回答**：

| 类型 | 描述 | 适用场景 |
|------|------|---------|
| **Single Agent** | 一个 LLM 完成所有任务 | 简单 QA、代码分析 |
| **Multi-Agent** | 多个 LLM 各自分工协作 | 复杂任务分解 |
| **ReAct Agent** | 推理 → 行动 → 观察循环 | 工具调用场景 |
| **Plan & Execute** | 先规划整体步骤，再逐部执行 | 长期任务 |
| **Reflexion Agent** | 执行后自我反思修正 | 代码生成、调试 |
| **Tool-using Agent** | 聚焦工具调用能力 | API 编排 |

**我项目的实践**：实现了两种

1. **Single Agent**（`agent.py`）：ReAct 循环，适合短平快分析
2. **Multi-Agent**（`langgraph_orchestrator.py`）：Plan & Execute + Review 三阶段管线

---

### 4. Agent 的 Tool Calling / Function Calling 机制是怎样的？

**问题**：解释 Tool Calling 的工作流程，和 function calling 的关系。

**回答**：

Tool Calling（也叫 Function Calling）是 LLM **自主决定调用哪个函数**的能力。流程：

```
                    ┌──────────────────────────┐
                    │     LLM 收到 user query     │
                    │     + tool definitions      │
                    └──────────┬───────────────┘
                               │ LLM 推理
                               ▼
                    ┌──────────────────────────┐
                    │  LLM 输出 tool_call:       │
                    │  {name: "read_file",      │
                    │   args: {"file_path": X}} │
                    └──────────┬───────────────┘
                               │ 解析参数
                               ▼
                    ┌──────────────────────────┐
                    │  执行本地函数 read_file(X) │
                    └──────────┬───────────────┘
                               │ 返回结果
                               ▼
                    ┌──────────────────────────┐
                    │  结果作为 tool_response    │
                    │  塞回 messages             │
                    └──────────┬───────────────┘
                               │ LLM 继续推理
                               ▼
                      ┌── 还要调工具？──┐
                      │ YES      NO    │
                      │          ↓      │
                      │        返回最终答案
```

**工具定义格式**：

```python
ToolDefinition(
    name="read_file",                    # ① 工具名：LLM 通过这个名字决定"我要调哪个工具"
    description="Read file content / 读取文件内容",  # ② 描述：LLM 唯一的决策依据
    parameters={                         # ③ JSON Schema：约束参数类型和结构
        "type": "object",
        "properties": {
            "file_path": {"type": "string", "description": "文件路径"},
            "start_line": {"type": "integer"},
        },
        "required": ["file_path"],       # file_path 必传，start_line 可选
    },
    func=read_file  # ④ 实际 Python 函数引用（不是字符串！）
)
```
```python
def _get_tool_schemas(self):
    """把 ToolDefinition 列表转成 LLM API 能理解的 JSON 格式"""
    return [{
        "type": "function",
        "function": {
            "name": t.name,              # LLM 看到这个决定调哪个
            "description": t.description, # LLM 理解工具用途
            "parameters": t.parameters,   # JSON Schema 告诉 LLM 传什么参数
        }
    } for t in self.tools.values()]
```
    
**关键难点**：
- 参数可能是 JSON 字符串（OpenAI 格式）或 dict（Anthropic 格式），需要统一处理：`json.loads(raw_args) if isinstance(raw_args, str) else raw_args`
- 流式场景下参数是 JSON 片段，需要解析器逐步拼接

---

### 5. Agent 的 Tools 应该怎么设计？有哪些原则？

**问题**：设计 Agent 工具集时有什么原则和注意点？

**回答**：

五大原则：

1. **粒度适中**：工具太粗（一个工具做太多事）→ LLM 难以理解；太细（每行代码一个工具）→ 调用链过长
2. **描述清晰**：name 和 description 是 LLM 理解工具的唯一方式，务必写清楚
3. **参数可校验**：用 JSON Schema 约束参数类型，LLM 经常幻觉参数名
4. **安全边界**：危险操作（rm -rf）用 HITL Guard 拦截
5. **错误可恢复**：工具失败不要崩，返回错误信息让 LLM 自己决定重试还是放弃

**例子**：项目中的 7 个工具

```
read_file     # 读文件        ← 只读，SAFE
list_files    # 列出目录      ← 只读，SAFE
grep_pattern  # 搜索代码      ← 只读，SAFE
run_command   # 执行命令      ← 危险，HITL 拦截
clone_repo    # 克隆仓库      ← 只读网络，SAFE
get_diff      # Git diff      ← 只读，SAFE
search_code   # 向量搜索      ← 只读，SAFE
```

**面试话术**：*"工具的 name/description 就是 LLM 的 API 文档——它没有别的办法知道这个工具是干什么的。我见过 LLM 把 ls 参数传给 rm，就是因为 description 写得不够清楚。"*

---

## 二、Agent Harness 实现（6-12）

### 6. Agent Harness 是什么？包含哪些组件？

**问题**：你项目的 Agent Harness 是什么？有哪些核心组件？

**回答**：

Agent Harness 是**管理 Agent 完整生命周期的运行时框架**，包含：

```
┌─────────────────────────────────────────┐
│            Agent Harness                 │
│                                          │
│  ┌─────────────────────────────────────┐ │
│  │      Execution Loop                 │ │
│  │   while turn < max_turns:           │ │
│  │     LLM.chat() → parse → tool_exec  │ │
│  │     → observe → loop_result/return  │ │
│  └─────────────────────────────────────┘ │
│                                          │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐   │
│  │Stream│ │Memory│ │Cache │ │HITL  │   │
│  │Parser│ │      │ │      │ │Guard │   │
│  └──────┘ └──────┘ └──────┘ └──────┘   │
│  ┌──────┐ ┌──────┐ ┌──────┐            │
│  │Sandbx│ │Telem │ │ JWT  │            │
│  │      │ │-etry │ │ Auth │            │
│  └──────┘ └──────┘ └──────┘            │
└─────────────────────────────────────────┘
```

**八大组件**（我的项目 `src/harness/`）：

| 组件 | 文件 | 职责 |
|------|------|------|
| Execution Loop | `agent.py` | 主循环，协调所有组件 |
| Streaming Parser | `streaming.py` | 流式 JSON 解析状态机 |
| Sandbox | `sandbox.py` | 进程隔离执行环境 |
| Telemetry | `telemetry.py` | 结构化日志 + 指标 |
| LLM Cache | `llm_cache.py` | LRU+TTL 响应缓存 |
| HITL Guard | `auth.py` | 人工审批危险操作 |
| Context Memory | `memory.py` | 三种记忆压缩策略 |
| JWT Auth | `jwt_auth.py` | 认证授权 |

---

### 7. Execution Loop 怎么实现的？关键代码是什么？

**问题**：手写一个 Agent 执行循环，关键代码和设计要点。

**回答**：

核心代码（精简版）：

```python
class AgentHarness:
    def run(self, task: str) -> str:
        # ① 用户任务加入对话历史
        self.messages.append({"role": "user", "content": task})

        # ② 主循环：每次迭代 = 一次 LLM 调用 + 若干工具执行
        for turn in range(self.max_turns):
            # ── 步骤 1：上下文压缩 ──
            # 如果 messages 太长就截断/摘要，防止超过 LLM 窗口
            if self.memory:
                self.messages = self.memory.compress(self.messages)

            # ── 步骤 2：查缓存 ──
            # 用当前 messages 的 SHA256 hash 做 key 查历史
            # 命中 → 直接返回（省一次 API 调用+钱）
            if self.cache:
                cached = self.cache.get(self.messages, self.model)
                if cached:
                    self.messages.append({"role": "assistant", "content": cached})
                    return cached

            # ── 步骤 3：调用 LLM ──
            # 传工具定义 → LLM 决定调不调、调哪个
            response = self.model.chat(
                messages=self.messages,
                tools=self.tools_defs if self.tools else None,
            )

            # ── 步骤 4：解析结果 ──
            if response.tool_calls:
                # LLM 决定调工具 → 交 _execute_tool_calls 处理
                self._execute_tool_calls(response.tool_calls)
            else:
                # LLM 输出文本答案 → 记入历史并返回
                self.messages.append({"role": "assistant", "content": response.content})
                return response.content

        # ③ 兜底：max_turns 用完还没 return → 强制结束
        return "Max turns reached"

    def _execute_tool_calls(self, tool_calls: list):
        """逐个执行 LLM 要求的工具调用"""
        for tc in tool_calls:
            # ── 第一关：HITL 安全检查 ──
            # 危险操作（rm -rf）先问人类，批准才放行
            if self.hitl and self.hitl.needs_approval(tc.name, tc.args):
                if not self.hitl.request_approval(tc.name, tc.args):
                    result = "Blocked by HITL"
                    continue

            # ── 第二关：沙箱隔离 ──
            # run_command 在临时目录 + 线程 + timeout 中执行
            if self.sandbox and tc.name == "run_command":
                result = self.sandbox.run(tc.args["command"])
            else:
                # 普通工具（read_file/list_files）直接执行
                result = tc.func(**tc.args)

            # ── 工具结果写回对话历史 ──
            # LLM 下一轮就能看到"我 ls 看到了哪些文件"
            self.messages.append({"role": "tool", ...})
```

**设计要点**：

1. **max_turns 兜底**：防止无限循环
2. **记忆压缩时机**：每轮调用前，不是调用后
3. **HITL 优先**：危险操作先阻断再执行
4. **错误不崩溃**：工具异常返回消息，让 LLM 决定重试

---

### 8. Streaming Parser 怎么处理流式 tool call？

**问题**：流式输出中 tool call 的 JSON 参数是分块到达的，怎么处理？

**回答**：

核心难点：LLM 返回的 tool call JSON 不是一次性完整的，而是 `"file"` → `"_path"` → ``"` → `"./s` → `"rc/"` 这种片段。

**解法**：状态机逐块拼接

```python
class ParserState(Enum):
    """流式解析器状态：LLM 返回的内容是逐块到达的，需要状态机拼装"""
    IDLE = 0                # 初始状态，等待 chunk 到达
    COLLECTING_TEXT = 1     # 正在收文本片段（LLM 在打字回复）
    COLLECTING_TOOL_NAME = 2  # 正在收工具名
    COLLECTING_ARGS = 3     # 正在收 JSON 参数片段（可能分 20 块才拼完）

class StreamingParser:
    def __init__(self):
        self.state = ParserState.IDLE
        self.tool_name = ""   # 累积的工具名
        self.tool_args = ""   # 累积的参数字符串（JSON 片段）
        self.text = ""        # 累积的文本内容

    def feed(self, chunk: Chunk) -> list[Event]:
        """
        每次收到一块数据就喂给解析器。
        返回解析出的 Event 列表（可能 0 个或多个）。
        """
        events = []

        # ── 情况 1：内容块有增量更新 ──
        if chunk.type == "content_block_delta":
            if chunk.delta.type == "text_delta":
                # LLM 在输出文字（比如 "我正在分析..."）
                self.state = ParserState.COLLECTING_TEXT
                self.text += chunk.delta.text
                events.append(Event("text", chunk.delta.text))  # ← 前端逐字渲染

            elif chunk.delta.type == "input_json_delta":
                # LLM 在输出 tool call 的 JSON 参数（分块到达）
                # 例如 chunks = ['{"file_path": "', 'agent.py"}']
                self.state = ParserState.COLLECTING_ARGS
                self.tool_args += chunk.delta.partial_json  # ← 累加 JSON 片段
                events.append(Event("tool_args", chunk.delta.partial_json))

        # ── 情况 2：内容块结束了 → JSON 应该完整了 ──
        elif chunk.type == "content_block_stop":
            if self.tool_name:
                # 拼接完整的 JSON 参数
                args = json.loads(self.tool_args)  # ← 将 '{"file_path":"agent.py"}' 解析成 dict
                events.append(Event("tool_call", {"name": self.tool_name, "args": args}))
                # 重置状态，准备处理下一个 tool call
                self.tool_name = ""
                self.tool_args = ""
            self.state = ParserState.IDLE  # 回到空闲

        return events
```

**面试话术**：*"流式解析的核心是用状态机维护 JSON 片段的拼装状态——IDLE 等待开始、COLLECTING 累加片段、JSON_READY 触发回调。不需要完整 JSON 就能逐段渲染文字，体验好很多。"*

---

### 9. Memory / Context 管理有哪些策略？

**问题**：Agent 的上下文窗口是有限的，怎么管理长对话记忆？

**回答**：

三种主流策略（我项目 `memory.py` 实现了全部）：

**① 滑动窗口**（Sliding Window）

```python
# 保留最后 N 轮对话，前面的全部丢掉
# - messages 是 [sys, user1, asst1, tool1, user2, asst2, ...]
# - window_size=10 表示保留最近 10 轮（即 20 条消息）
messages = messages[-window_size * 2:]
```

- 优点：简单、零成本
- 缺点：早期关键信息丢失

**② LLM 摘要**（Summarization）

```python
# 除最新的几轮外，前面全让 LLM 压缩成一段摘要
# 用一次额外的 LLM 调用换取信息保留
summary_prompt = "Summarize the above conversation concisely:"
summary = llm.chat(summary_prompt + full_text)
# 用摘要消息替换掉早期历史，保留最新几轮
messages = [summary_message] + recent_messages
```

- 优点：保留语义信息
- 缺点：额外 LLM 调用、有损压缩

**③ 混合策略**（Hybrid — 我项目默认）

```python
if len(messages) > window_size:
    # 保留头尾无损，只压缩中间重复的部分
    head = messages[:3]           # 保留：system prompt + 初始任务
    tail = messages[-(window_size - 3):]  # 保留：最近 N 轮（细节不能丢）
    middle = messages[3:-window_size]     # 压缩：中间的重复调用来回
    compressed = self._summarize(middle) if middle else []
    return head + compressed + tail      # head 无损 + middle 摘要 + tail 无损
```

- 优点：保留上下文连续性，压缩历史冗余
- 缺点：实现复杂一些

**面试话术**：*"上下文窗口是 Agent 的核心瓶颈之一。工业界实践中，混合策略最常用——保留首尾无损、压缩中间冗余，兼顾准确率和 token 成本。"*

---

### 10. LLM Cache 怎么实现？为什么需要？

**问题**：LLM 输出为什么需要缓存？怎么实现？

**回答**：

**为什么需要**：LLM API 调用有三大痛点
- **高延迟**：首次调用 3-10s
- **高成本**：按照 token 计费
- **非确定性**：相同输入可能不同输出（但很多场景稳定更重要）

**实现**（`llm_cache.py`）：

```python
class LLMCache:
    def __init__(self, max_size=200, ttl_seconds=300):
        self.cache = OrderedDict()    # ① OrderedDict 实现 LRU：最近用的放最后
        self.ttl = ttl_seconds         # ② TTL 过期：默认 5 分钟
        self.max_size = max_size       # ③ 最大条目数：防止内存爆炸

    def _make_key(self, messages, model):
        """生成缓存 key：messages 的 SHA256 + 模型名"""
        # 只用最近 4 条消息做 key（全量 messages 太长，做 key 浪费）
        relevant = messages[-4:] if len(messages) > 4 else messages
        content = json.dumps(relevant, sort_keys=True)
        return hashlib.sha256(content.encode()).hexdigest() + ":" + model

    def get(self, messages, model):
        """查缓存：命中且没过期 → 返回；过期或没命中 → None"""
        key = self._make_key(messages, model)
        if key in self.cache:
            timestamp, content = self.cache[key]
            if time.time() - timestamp < self.ttl:
                self.cache.move_to_end(key)  # LRU：刚用过，移到末尾
                return content
            else:
                del self.cache[key]  # TTL 过期，清除
        return None
```

**命中场景**：
- Agent 多次分析相同文件 → 二次秒开
- Multi-Agent 中 Planner 和 Executor 查相同代码 → 共享缓存
- 调试阶段反复同 Prompt → 节省大量 token

**面试话术**：*"LRU+TTL 组合是最实用的缓存策略——LRU 防止热点查询撑爆内存，TTL 保证数据不过时。注意 cache key 不要用全部 messages（太长），取最后几轮足够。"*

---

### 11. HITL（Human-in-the-Loop）怎么设计？

**问题**：如何在 Agent 中实现人机协同审核？

**回答**：

**分级审核架构**：

```python
class RiskLevel(Enum):
    """三级风险模型：决定工具调用需要什么级别的审批"""
    SAFE = 0       # ✅ 自动通过：read_file, list_files（只读操作，无风险）
    MODERATE = 1   # ⚠️ 需确认：write_file, git commit（写操作，可能影响代码）
    DANGEROUS = 2  # 🛑 阻塞：rm -rf, shutdown（破坏性操作，默认拒绝）

class HumanInTheLoop:
    def needs_approval(self, tool_name, args) -> bool:
        """判断是否需要人类审批"""
        risk = self._assess_risk(tool_name, args)
        return risk != RiskLevel.SAFE  # 不是 SAFE 就需要审批

    def request_approval(self, tool_name, args) -> bool:
        """执行审批：True = 批准放行，False = 拒绝"""
        risk = self._assess_risk(tool_name, args)
        if self.approval_callback:
            # 生产模式：通过回调弹 UI / 发 Slack 等人类点头
            return self.approval_callback(tool_name, args, risk)
        # 开发模式：DANGEROUS 拒绝，MODERATE 通过
        return risk == RiskLevel.MODERATE
```

**集成到 Execution Loop**：

```python
if self.hitl and self.hitl.needs_approval(tc.name, tc.args):
    approved = self.hitl.request_approval(tc.name, tc.args)
    if not approved:
        result = f"⚠️ Tool '{tc.name}' was blocked by admin review"
        self.messages.append({"role": "tool", "content": result})
        continue  # 跳过执行，给 LLM 解释机会
```

**面试话术**：*"HITL 是 Agent 安全性的最后一道防线。工业界把工具调用分成三个风险等级——只读的自动放行、写操作需要确认、危险操作默认拒绝。这样既保证效率，又防止 Agent 干出不可控的事。"*

---

### 12. Sandbox 进程隔离怎么实现的？

**问题**：Agent 执行命令时怎么保证安全？

**回答**：

**实现**（`sandbox.py`）：

```python
class Sandbox:
    def __init__(self, timeout=30, temp_dir=None):
        self.timeout = timeout           # 命令执行超时（秒）
        self.temp_dir = temp_dir or tempfile.mkdtemp()  # 隔离目录

    def run(self, command: str) -> str:
        """在隔离环境中执行命令，超时强制终止，不影响宿主"""
        try:
            result = subprocess.run(
                command,                # LLM 想执行的命令（如 "ls -la"）
                shell=True,             # ✅ 方便但要注意注入（生产环境用 shlex）
                capture_output=True,    # 捕获 stdout + stderr（不回显到终端）
                text=True,              # 返回字符串而非 bytes
                timeout=self.timeout,   # ⏱ 超过 30s 就强制杀掉子进程
                cwd=self.temp_dir,      # 📁 在临时目录执行，影响不到外面
            )
            return result.stdout + result.stderr  # 合并输出给 LLM 看
        except subprocess.TimeoutExpired:
            return f"Timeout ({self.timeout}s)"  # 超时不崩溃，返回友好信息
```

**三层安全隔离**：

| 层级 | 措施 | 保护目标 |
|------|------|---------|
| **进程级** | subprocess 独立进程 | Agent 主进程不崩溃 |
| **目录级** | `cwd=temp_dir` | 不影响宿主文件 |
| **时间级** | timeout 兜底 | 防止死循环 |
| **审计级** | HITL 前置检查 | 禁止危险命令 |

**面试话术**：*"Sandbox 不是 100% 安全的——真正的沙箱需要 Docker 容器 + seccomp + no-network。我这个是进程级隔离，适合开发和内网环境。面试官如果问到，我会补充 Docker 容器的隔离方案。"*

---

## 三、多 Agent 与编排（13-17）

### 13. Multi-Agent 系统是什么？和 Single Agent 比有什么优劣？

**问题**：为什么需要多个 Agent？和单个 Agent 比有什么优劣？

**回答**：

**Single Agent 瓶颈**：
- 一个 prompt 要做所有事 → 上下文超长 → 性能下降
- 工具集太大 → LLM 选择困难
- 没有校验环节 → 错误传播到最终输出

**Multi-Agent 优势**：

| 维度 | Single Agent | Multi-Agent |
|------|-------------|-------------|
| 分工 | 一个人干所有 | 各司其职，专业度高 |
| 上下文 | 全部塞一个窗口 | 各自处理自己的部分 |
| 错误控制 | 无人校验 | Reviewer 可以纠正 Executor |
| 可扩展性 | 加功能就得改 prompt | 加 Agent 就行 |

**Multi-Agent 劣势**：
- **更慢**：多个 LLM 顺序调用
- **更贵**：多轮 API 调用
- **更复杂**：Agent 间通信、状态同步

**我的项目实践**：

```
Planner（发现）→ Executor（验证）→ Reviewer（报告）

找到 31 个潜在问题 → 验证后 12 个 CONFIRMED → 去重成 8 个最终报告
```

---

### 14. 你的 Multi-Agent 系统是怎么组织的？

**问题**：具体描述你的 Multi-Agent 管线。

**回答**：

三阶段管线：

```python
class LangGraphOrchestrator:
    def _build_graph(self):
        # ① 定义状态图：AgentState 是跨节点的全局状态（findings, messages, phase...）
        w = StateGraph(AgentState)

        # ② 注册三个节点：每个节点 = 一个独立 Agent
        w.add_node("plan", self._plan_node)        # Phase 1: 规划 → 发现所有潜在问题
        w.add_node("execute", self._execute_node)  # Phase 2: 执行 → 逐条验证真伪
        w.add_node("review", self._review_node)    # Phase 3: 审核 → 去重输出报告

        # ③ 条件边：Plan 之后决策走向
        # 有发现 → execute（继续）；无发现 → end（直接结束，省一轮 LLM）
        w.add_conditional_edges(
            "plan",
            self._should_continue,  # 路由函数：返回 "execute" 或 "end"
            {"execute": "execute", "end": END}
        )

        # ④ 固定边：execute 完必须 review，review 完结束
        w.add_edge("execute", "review")
        w.add_edge("review", END)

        # ⑤ 入口和编译
        w.set_entry_point("plan")
        return w.compile()  # 编译成可执行的图
```

| Phase | Agent | 工具 | 职责 |
|-------|-------|------|------|
| **Plan** | Planner | list_files, read_file, grep | 理解项目，发现潜在问题 |
| **Execute** | Executor | grep, read_file | 逐条验证，标记 CONFIRMED/FP |
| **Review** | Reviewer | read_file, grep | 去重合并，输出报告 |

**数据流**：

```
User Task
   │
   ▼
Planner → [FINDING|file|line|severity|desc|suggestion]
   │                  ↓
   ▼
Executor → [VERDICT|id|CONFIRMED/FALSE_POSITIVE|evidence]
   │                  ↓
   ▼
Reviewer → Final Report (Markdown)
```

---

### 15. LangGraph 和硬编码管线有什么区别？

**问题**：为什么要用 LangGraph？和直接顺序调用有什么区别？

**回答**：

**硬编码管线**（`orchestrator.py`）：

```python
# 硬编码 = 线性执行，没有分支，没有条件跳过
# 不管 Planner 有没有发现，Executor 和 Reviewer 都得跑一轮
result_a = agent_a.run(task)    # Phase 1: Planner → 固定跑
result_b = agent_b.run(result_a)  # Phase 2: Executor → 必跑（没发现也跑）
result_c = agent_c.run(result_b)  # Phase 3: Reviewer → 必跑
```

**LangGraph 图编排**（`langgraph_orchestrator.py`）：

```python
# LangGraph = 图结构 + 条件边 + 全局状态管理
graph = StateGraph(AgentState)            # 全局状态：findings, messages, phase...
graph.add_node("plan", plan_node)         # 节点 1：Plan
graph.add_node("execute", execute_node)   # 节点 2：Execute
graph.add_node("review", review_node)     # 节点 3：Review
graph.add_conditional_edges("plan", should_continue, {
    "execute": "execute",  # findings > 0 → 去验证
    "end": END,            # findings = 0 → 直接结束，不浪费 token
})
graph.add_edge("execute", "review")       # 验证完必须审核
```

**核心差异**：

| 维度 | 硬编码 | LangGraph |
|------|--------|-----------|
| 控制流 | 线性不可变 | 图 + 条件边 + 循环 |
| 状态管理 | 手动传参 | AgentState 全局状态 |
| 扩展性 | 加阶段得改链 | 加节点加边即可 |
| 调试 | print | LangSmith 追踪 |它通过记录应用在一次请求中执行的每一个步骤，为你提供端到端的全方位可观测性。这对于调试像 AI Agent 这类逻辑复杂、非确定性的系统来说，是至关重要的基础设施。

**条件路由示例**：

```python
def should_continue(state: AgentState) -> str:
    """
    路由函数：Planner 执行完毕后判断下一步走哪个边。
    - 返回 "execute" → 走 execute 分支（有发现需要验证）
    - 返回 "end"     → 走 end 分支（没发现直接结束）
    """
    if state.get("findings") and len(state["findings"]) > 0:
        return "execute"    # 有发现 → 去验证
    return "end"            # 没发现 → 直接结束，省一次 LLM 调用
```

**面试话术**：*"硬编码管线在阶段少的时候够用，但当你有条件分支（有发现/无发现、需要重新规划）、循环（Reviewer 不满意退回重做）时，图编排的威力就出来了——LangGraph 让 Agent 间的拓扑关系看得见、改得动。"*

---

### 16. Agent 间通信怎么设计的？信息丢失怎么处理？

**问题**：多个 Agent 之间怎么传递信息？会不会丢？

**回答**：

**通信方式**：

```python
# ─── Phase 1 → Phase 2: 结构化文本传递 ───
# Planner 输出 "FINDING|file|line|severity|desc|suggestion" 格式
# 用 | 分隔符保证机器可解析，不会因为 LLM 表述不同而丢失语义
planner_output = planner.run(task)
findings = parse_findings(planner_output)  # 正则解析 FINDING| 行 → 结构化 dict 列表

# ─── Phase 2 → Phase 3: JSON + 自然语言 ───
# 把发现序列化为 JSON 塞进 Executor 的输入 prompt
executor_input = json.dumps(findings, indent=2)
executor_output = executor.run("Verify:\n" + executor_input)
verdicts = parse_verdicts(executor_output)  # 解析 VERDICT| 格式

# ─── 合并给 Reviewer ───
# 把 findings + verdicts 都塞进 Reviewer 的 prompt
final_report = reviewer.run(
    f"Findings ({len(findings)}):\n" +
    json.dumps(findings, indent=2) +        # 所有发现的完整 JSON
    "\n\nProduce report."                    # Reviewer 用自己的 prompt 生成最终报告
)
```

**信息丢失防护**：

| 策略 | 实现 |
|------|------|
| **结构化输出** | FINDING\| 格式 = 机器可解析，不会"理解偏差" |
| **冗余传递** | 每个 phase 输入都包含完整上下文 |
| **Schema 校验** | parse_verdicts() 检查字段完整，缺失打日志 |
| **Fallback** | 解析失败时保留原文不丢数据 |正则解析做"提取"而非"截断"——不匹配的行被跳过但留在原文里，字段不够补空不丢弃，整体为空优雅返回不崩溃。原始 LLM 输出全程保留在 messages 里。

**面试话术**：*"Agent 间通信最怕的是语义丢失——Agent B 误解了 Agent A 的输出。我的解法是用结构化格式（FINDING|）传递，解析失败也能保留原文，不会静默丢数据。"*

---

### 17. Multi-Agent 的性能怎么评估？

**问题**：怎么衡量 Multi-Agent 系统好不好？

**回答**：

**评估指标**：

```python
# 评估指标的三个核心公式
metrics = {
    "precision": confirmed / (confirmed + false_positive),
    #  精准率 = 找的对不对？ 1.0 = 找到的每个都是真bug，0.5 = 一半是误报
    "recall": confirmed / total_bugs,
    #  召回率 = 找到了多少？ 0.6 = 10个bug找到了6个
    "f1": 2 * precision * recall / (precision + recall),
    #  F1 = 精准率和召回率的调和平均数，综合评价
}
```

**我的项目实测**（Eval: 10 bugs × 6 files）：

| 指标 | 值 |
|------|-----|
| Recall | 6/10 = **60%** |
| Precision | 6/6 = **100%** |
| 误报 | **0** |

**Agent 强项**：明显代码缺陷（裸 except、竞态条件、缺少异常处理）
**Agent 弱项**：语义层面问题（逻辑陷阱、环境变量副作用）

**更多评估维度**：

| 维度 | 指标 | 测量方式 |
|------|------|---------|
| 正确性 | Precision / Recall / F1 | 标注数据集对比 |
| 效率 | 耗时 / Token 消耗 | 打点统计 |
| 鲁棒性 | 相同输入多次输出一致性 | 多轮跑 |
| 覆盖率 | 工具使用率 / 文件覆盖率 | 日志分析 |

---

## 四、LLM 原理与 API（18-23）

### 18. Prompt Caching 是什么？怎么用的？

**问题**：解释 Anthropic 的 Prompt Caching 机制和你的用法。

**回答**：

**Prompt Caching**：当多个请求有**相同前缀**（如 system prompt、工具定义）时，服务端缓存该部分的 KV 状态，后续请求只计算差异部分。

```
第一次请求:
  system: [已缓存 ⏳] "你是代码分析专家..."   ← 占用缓存
  user:   "分析 agent.py"

第二次请求（相同 system prompt）:
  system: [缓存命中 ⚡] "你是代码分析专家..."  ← 免费
  user:   "分析 streaming.py"
```

**我的用法**（`llm_client.py`）：

```python
# ① System prompt 标记为缓存
# cache_control: ephemeral 告诉 API：这段内容可以被缓存
# 下次相同 system prompt 的请求直接复用 KV cache
payload["system"] = [{
    "type": "text",
    "text": system_prompt,
    "cache_control": {"type": "ephemeral"}  # ← 缓存标记（关键！）
}]

# ② 工具定义的最后一个也标记缓存
# 三个 Agent（Planner/Executor/Reviewer）共享相同的工具定义
# 第一个 Agent 触发缓存后，后面两个几乎零成本
for i, t in enumerate(tool_defs):
    if i == len(tool_defs) - 1:  # 最后一个工具定义做 cache breakpoint
        t["cache_control"] = {"type": "ephemeral"}
```

**效果**：

```
测试输出:
  LLM with caching: OK
  Prompt caching: active (system + tools marked cache_control=ephemeral)
```

**面试话术**：*"Prompt caching 在 Multi-Agent 场景收益最大——三个 Agent 共享相同的 system prompt 和工具定义，第一个 Agent 触发完整计算后，后面两个几乎免费。"*

---

### 19. System Prompt 怎么写？不同 Agent 有什么区别？

**问题**：你的项目中 Planner / Executor / Reviewer 的 System Prompt 各有什么特点？

**回答**：

**Planner 提示词**（激进探索）：

```
You are a code review planner. Your job is to find EVERY potential issue.
- Read the project files thoroughly
- For EACH file, output 2+ FINDING lines
- NEVER output NO_FINDINGS — there is always something
- Output format: FINDING|file|line|category|severity|description|suggestion
```

**Executor 提示词**（谨慎验证）：

```
You are a code review executor. Verify each finding.
- Use grep to search for the relevant code
- Check if the described bug actually exists
- Be honest: CONFIRMED only if you have evidence
- FALSE_POSITIVE if the code is actually fine
- UNCERTAIN if you cannot determine
- Output: VERDICT|finding_id|CONFIRMED/FALSE_POSITIVE/UNCERTAIN|Evidence
```

**Reviewer 提示词**（综合分析）：

```
You are a code review reviewer. Produce the final report.
- Deduplicate similar findings
- Sort by severity (CRITICAL > HIGH > MEDIUM > LOW)
- Only include CONFIRMED in main section
- Put UNCERTAIN in a separate section
- List FALSE_POSITIVE with brief explanation
- Output as clean bilingual markdown
```

**设计原则**：

| Agent | 角色 | 心态 | 工具集 | 轮次限制 |
|-------|------|------|--------|---------|
| Planner | 扫描器 | **激进**：宁可误报不可漏报 | 全部工具 | 8 |
| Executor | 验证器 | **保守**：没证据就是 FP | grep/read | 8 |
| Reviewer | 编辑 | **客观**：去重/分类/排序 | read/grep | 5 |

---

### 20. XML Tool Call 解析是什么？为什么需要？

**问题**：你的项目为什么需要 XML 格式的 tool call 解析？

**回答**：

**背景**：DeepSeek v4 的 Anthropic 兼容 API 在 tool calling 上行为特殊——有时返回标准 JSON 格式的 `tool_use` block，有时在 text 中返回 XML 格式的 `<invoke>` 标签。

**XML 格式示例**：

```xml
我是助手。让我读取这个文件。

<invoke name="read_file">
<parameter name="file_path">./src/agent.py</parameter>
<parameter name="start_line">1</parameter>
<parameter name="end_line">50</parameter>
</invoke>

让我看看文件开头...
```

**解析器**（`llm_client.py`）：

```python
# 正则提取 XML 格式的 tool call
# DeepSeek v4 有时在 text 里返回这种格式：
# <invoke name="read_file"><parameter name="file_path">agent.py</parameter></invoke>
xml_pattern = re.compile(服务端缓存该部分的 KV 状态
    r'<invoke\s+name="(\w+)">(.*?)</invoke>', re.DOTALL  # 匹配 <invoke>...</invoke>
)
param_pattern = re.compile(
    r'<parameter\s+name="(\w+)">(.*?)</parameter>', re.DOTALL  # 匹配 <parameter>...</parameter>
)

for m in xml_pattern.finditer(block_text):
    tool_name = m.group(1)      # 提取工具名：read_file
    params_str = m.group(2)     # 提取参数块：...<parameter>...</parameter>...
    args = {}
    for pm in param_pattern.finditer(params_str):
        args[pm.group(1)] = pm.group(2).strip()  # file_path → "agent.py"
    tool_calls.append({"name": tool_name, "args": args})
```

**面试话术**：*"不同 LLM 的 tool calling 格式不统一——OpenAI 用 JSON 参数，Anthropic 用 tool_use block，DeepSeek 有时还混用 XML。一个健壮的 Agent 需要支持多种格式的解析，这就是我写 XML 解析器的原因。"*

---

### 21. 消息格式转换（OpenAI ↔ Anthropic）需要注意什么？

**问题**：项目中的 `_convert()` 和 `_conv_asst()` 是做什么的？

**回答**：

**`_convert()`**：把内部统一 `messages` 格式转成 Anthropic API 格式

```python
def _convert(self, messages):
    """
    把内部统一 messages 格式转成 Anthropic API 格式

    内部格式:
        [{"role":"system",...}, {"role":"user",...},
         {"role":"assistant","tool_calls":[...]}, {"role":"tool",...}]

    Anthropic 格式:
        system 提取到顶层参数
        role 只有 user/assistant，tool 合并在 user 的 content 里
    """
    sp, api = "", []
    for m in messages:
        if m["role"] == "system":
            sp = m["content"]                     # system 提取出来单独传
        elif m["role"] == "assistant":
            api.append(self._conv_asst(m))         # assistant 需要特殊处理（tool_calls 转格式）
        elif m["role"] == "user":
            api.append({"role": "user", "content": m["content"]})
        elif m["role"] == "tool":
            # ⚠️ Anthropic 没有 tool role！连续 tool 结果合并成一条 user 消息
            results = []
            while i < len(messages) and messages[i]["role"] == "tool":
                results.append({"type": "tool_result", ...})
                i += 1
            api.append({"role": "user", "content": results})
    return sp, api
```

```python
def _conv_asst(self, m):
    """
    把 assistant 消息转成 Anthropic 的 content blocks 格式

    🔑 最大坑点：OpenAI 的 tool_call.arguments 是 JSON 字符串！
    Anthropic 的 tool_use.input 是 dict！
    不转换的话 LLM 收到的是字符串 '{...}' 而不是解析好的 dict
    """
    tcs = m.get("tool_calls") or []
    text = m.get("content")
    blocks = []
    if text:
        blocks.append({"type": "text", "text": text})  # 文本部分

    for tc in tcs:
        raw_args = tc.get("function", tc).get("arguments", {})
        # 🔑 关键行：OpenAI 格式是 str，Anthropic 格式是 dict
        if isinstance(raw_args, str):
            raw_args = json.loads(raw_args)  # 反序列化：'{"key":"val"}' → {"key":"val"}
        blocks.append({
            "type": "tool_use",
            "id": tc["id"],
            "name": tc["function"]["name"],
            "input": raw_args               # Anthropic 要求 dict，不是 str
        })
    return {"role": "assistant", "content": blocks}
```

**面试话术**：*"最大的坑是参数格式——OpenAI 的 `arguments` 是 JSON 字符串，Anthropic 的 `input` 直接是 dict。不处理的话就会把字符串 `'{...}'` 当作文本传过去，LLM parse 不了直接报错。"*

---

### 22. Token 消耗怎么管理？有没有超长截断策略？

**问题**：LLM 上下文窗口有限，超长时怎么处理？

**回答**：

**Token 估算**：

```python
def estimate_tokens(text: str) -> int:
    """粗略估算 token 数，避免真的调 tiktoken（太慢）
    
    中文平均 1.5 token/字，英文约 0.25 token/字符
    足够用来判断"是不是快超限了"
    """
    chinese_chars = len(re.findall(r'[一-鿿]', text))  # 中文字符数
    ascii_chars = len(text) - chinese_chars              # 非中文字符数
    return int(chinese_chars * 1.5 + ascii_chars * 0.25)
```

**三层截断策略**：

| 策略 | 触发条件 | 操作 |
|------|---------|------|
| **Memory 压缩** | 轮次 > 10 | 叫 LLM 压缩中间对话 |
| **Truncate 中间** | 仍超限 | 截断中间消息，保留首尾 |
| **工具结果截断** | 单工具结果 > 2000 tokens | `.content[:2000] + "... (truncated)"` |

```python
# agent.py run_streaming() 中的截断逻辑
# ① 先截断单条工具结果（最大的 killer）
combined_text = ""
for m in self.messages:
    if m["role"] == "tool" and len(m["content"]) > 2000:
        # 单条工具 stdout 太长 → 截断到 2000 字符
        # 避免 LLM 把算力浪费在记日志而不是推理上
        m["content"] = m["content"][:2000] + "\n...(truncated)"
    combined_text += str(m["content"])

# ② 总消息还超限 → 删除中间对话轮次
if estimate_tokens(combined_text) > self.max_context:
    self._truncate_messages()
```

**面试话术**：*"Token 管理是 Agent 工程的核心问题。工业实践是三层兜底——Memory 压缩保语义、消息截断保长度、工具结果截断保核心内容。不够优雅但有效。"*

---

### 23. Model Router 是什么？为什么要路由模型？

**问题**：你的 `model_router.py` 是做什么的？什么场景需要路由？

**回答**：

**Model Router**：根据任务复杂度，动态选择不同 LLM 模型，平衡成本和质量。

```python
class ModelRouter:
    """按任务关键字动态路由到不同模型，平衡成本和质量"""
    RULES = [
        # 简单任务 → 便宜模型（$0.15/M tokens）
        ("grep|list|read", "deepseek-chat"),
        # 复杂推理 → 强模型（$2/M tokens，贵 13 倍但推理更强）
        ("analyze|review|audit|security", "deepseek-v4-pro"),
        # 兜底：弱模型
        (".*", "deepseek-chat"),
    ]

    def route(self, task: str) -> AnthropicClient:
        for pattern, model_name in self.RULES:
            if re.search(pattern, task, re.IGNORECASE):  # 正则匹配任务关键字
                return model_name  # 返回对应的模型名（不是 client 实例）
        return "deepseek-chat"
```

**路由收益**：

| 场景 | 模型选择 | 节省 |
|------|---------|------|
| 简单文件列表 | chat ($0.15/M) | vs pro ($2/M) ≈ **93%** |
| 复杂安全审计 | pro ($2/M) | 必须，无法节省 |
| 多 Agent 中的 Executor | chat | 验证工作不需要最强推理 |

**面试话术**：*"Model Router 是多模型策略的核心——不是所有任务都需要最强的模型。简单的 grep 搜索用 chat 就够了，只有复杂的源码分析才需要 pro。生产环境中这个路由逻辑可以对接成本监控仪表盘，动态调整路由阈值。"*

---

## 五、生产化与面试综合（24-30）

### 24. 你项目的 JWT 认证是怎么实现的？

**问题**：描述你的 JWT 认证系统设计。

**回答**：

见我项目完整实现：[jwt_auth.py](src/harness/jwt_auth.py)

**三层验证链**：

```python
async def get_current_user(credentials) -> User:
    """
    FastAPI 依赖注入：每个受保护请求自动调用此函数

    三层验证链，任一失败 → HTTP 401：
    """
    # 第 ① 层：HMAC-SHA256 签名校验
    # jose.jwt.decode 内部做签名验证，token 被篡改就抛 JWTError
    payload = jwt_auth.verify_token(token, token_type="access")

    # 第 ② 层：过期检查（内置在 decode 的 verify_exp=True 中）
    # 超 15 分钟 → ExpiredSignatureError

    # 第 ③ 层：吊销列表 O(1) 检查
    # 支持主动登出：登出时 jti 入黑名单
    if revocation_list.is_revoked(payload["jti"]):
        raise HTTPException(401, "Token revoked")

    # 全部通过 → 返回 User 对象，路由函数直接用
    return User(username=payload["sub"], role=payload.get("role"))
```

**双 Token 机制**：

| Token | 有效期 | 用途 | 泄露影响 |
|-------|--------|------|---------|
| Access Token | 15 min | 业务 API 鉴权 | 窗口小 |
| Refresh Token | 7 days | 仅用于刷新 access token | 只能换 token，不能调 API |

**密码安全**：

```python
# ── 注册时：bcrypt 慢哈希存储 ──
# gensalt() 自动生成随机盐值，每次注册结果不同
# 即使两个用户密码相同，存储的 hash 也不同
hash = bcrypt.hashpw(password.encode(), bcrypt.gensalt()).decode()

# ── 登录时：constant-time 比较 ──
# checkpw 内部用固定时间比较，防止时序侧信道攻击
# 攻击者无法根据响应时间差异猜密码长度/字符
bcrypt.checkpw(password.encode(), stored_hash.encode())
```

---

### 25. Rate Limiting 和防暴力破解怎么做的？

**问题**：API 层面有什么攻击防护措施？

**回答**：

**两层限流**：

```python
# ── 全局默认限流：100 请求/分钟 ──
# 基于 IP 地址（get_remote_address）
# 超过限制返回 HTTP 429 Too Many Requests
limiter = Limiter(key_func=get_remote_address,
                  default_limits=["100/minute"])

# ── 登录接口单独限流：10 请求/分钟 ──
# 比全局更严格，防止暴力破解密码
@app.post("/auth/login")
@limiter.limit("10/minute")
async def login(req: LoginRequest, request: Request):
    ...
```

**防暴力破解组合拳**：

| 防护层 | 措施 | 效果 |
|--------|------|------|
| IP 限流 | slowapi 10/min | 攻击者最多试 10 次/分钟 |
| bcrypt 慢哈希 | ~100ms/次 | 10 次/分钟 × 100ms = 几乎不可行 |
| 审计日志 | 记录登录失败 | 可溯源攻击来源 |

---

### 26. 你在项目里用了 Prometheus 做什么？

**问题**：Prometheus 在这个项目里监控了哪些指标？

**回答**：

**自动采集指标**（通过 `prometheus-fastapi-instrumentator`）：

```python
# 一行代码挂载 Prometheus HTTP 指标采集器
# 自动捕获：请求数 / 延迟 / 错误码 / 并发
# 暴露在 GET /metrics → Prometheus Server 定期抓取
Instrumentator().instrument(app).expose(app)
```

**70+ 自动指标**：

| 类别 | 指标示例 |
|------|---------|
| HTTP 请求数 | `http_requests_total{method, endpoint, status}` |
| 请求延迟 | `http_request_duration_seconds`（分桶） |
| 错误率 | `http_requests_total{status="5xx"}` |
| 并发数 | 当前处理中的请求 |

**自定义 Dashboard**（`/dashboard` 端点）：

```
┌──────────┬──────────┬──────────┬──────────┐
│ Total    │ Errors   │ API      │ Streamlit│
│ Requests │          │ Status   │ Status   │
│   2,341  │     12   │   ● UP  │   ● UP   │
└──────────┴──────────┴──────────┴──────────┘
```

**面试话术**：*"Prometheus 指标在面试中可能被问——'你如何监控你的 Agent 系统？'我的回答是三层：API 层（HTTP 指标）、Agent 层（每轮 token 消耗、工具调用次数）、业务层（发现数量、准确率）。"*

---

### 27. 向量搜索在项目中用了什么？为什么用 SQLite FTS5 而不用 ChromaDB？

**问题**：你项目的向量搜索是怎么实现的？为什么选 SQLite FTS5？

**回答**：

**实现**（`vector_store.py`）：SQLite FTS5 全文搜索

```python
class VectorStore:
    def __init__(self, db_path):
        self.conn = sqlite3.connect(db_path)
        # 创建 FTS5 虚拟表（SQLite 内置全文搜索，零依赖）
        # fts5 自动建索引，MATCH 查询比 LIKE 快 100x+
        self.conn.execute("""
            CREATE VIRTUAL TABLE IF NOT EXISTS findings_fts
            USING fts5(
                file_path, description_en, description_cn, suggestion,
                content=findings, content_rowid=rowid
            )
        """)

    def search(self, query, n_results=5):
        # 英文 → FTS5 MATCH（基于分词和词根，更精确）
        # 中文 → LIKE 降级（FTS5 中文分词效果差）
        if re.search(r'[一-鿿]', query):  # 检测是否含中文
            return self._like_search(query, n_results)
        return self._fts_search(query, n_results)
```

**为什么不用 ChromaDB**？

| 维度 | ChromaDB | SQLite FTS5 |
|------|----------|-------------|
| 安装 | pip install chromadb（ONNX 依赖大） | Python 内置 |
| Windows 兼容 | ONNX 在 Win 上 segfault | ✅ 完美运行 |
| 中文搜索 | 需要 embedding 模型 | LIKE 回退 |
| 部署复杂度 | 单独进程 | 零配置 |
| 适合场景 | 大规模语义搜索 | 中小规模全文搜索 |

**面试话术**：*"技术选型要贴合场景——我这个项目的搜索需求是'找已知关键字的发现'，FTS5 完全够用还零依赖。ChromaDB 更适合语义搜索场景（'类似这个 bug 的问题'），但那需要 embedding 模型和更多基础设施。"*

---

### 28. 项目的 CI/CD 怎么设计的？

**问题**：你的 GitHub Actions CI 工作流包含哪些步骤？

**回答**：

```yaml
# .github/workflows/ci.yml
name: CI
on: [push, pull_request]       # push 或 PR 都触发
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4     # 拉取代码
      - uses: actions/setup-python@v5  # 装 Python
        with: { python-version: "3.11" }

      # ① 代码风格检查（快速失败，几秒就出结果）
      - name: Lint
        run: ruff check src/

      # ② 类型检查（静态分析，找隐蔽的类型错误）
      - name: Type check
        run: mypy src/ --ignore-missing-imports

      # ③ 单元测试（真正的功能验证）
      - name: Test
        run: pytest tests/ -v --tb=short

      # ④ 覆盖率报告（看测试覆盖了多少代码）
      - name: Coverage
        run: pytest --cov=src tests/ --cov-report=term

      # ⑤ Eval 阈值（回归检查：如果改了代码导致精准率下降，CI 直接挂）
      - name: Eval check
        run: python tests/eval_dataset.py --min-precision 0.5
```

**Pre-push Hook**（`push.bat`）：

```bash
# push.bat — 推前检查：测试没过就别想 push
pytest tests/ -q           # ① 先跑测试（安静模式）
if %errorlevel% neq 0 exit /b %errorlevel%  # ② 测试失败 → 阻止推送
git push                    # ③ 测试全过 → 才推送
```

**面试话术**：*"CI 不是简单的'跑测试'，而应该是五道门：lint 拦格式、mypy 拦类型、pytest 拦逻辑错误、coverage 拦测试不足、eval 拦性能回归。任何一道没过，PR 不允许 merge。"*

---

### 29. 这个项目的 Evaluation 是怎么做的？

**问题**：你如何评估你的 Agent 效果？

**回答**：

**标注数据集**（`tests/eval_dataset.py`）：33 条手工标注样本

| 类别 | 数量 | 示例 |
|------|------|------|
| 🐛 BUG | 22 | except: pass 吞异常、竞态条件 |
| 🔒 SECURITY | 5 | 硬编码密钥、注入风险 |
| ⚡ PERFORMANCE | 5 | 不必要循环、N+1 查询 |
| 📝 STYLE | 1 | 命名不规范 |

**评估指标**：

```python
def evaluate(predictions, ground_truth):
    """
    评估 Agent 准确率的标准指标

    参数:
        predictions: Agent 找到的 bugs 列表  (=["except:pass 吞异常"])
        ground_truth: 人工标注的真实 bugs   (=["except:pass 吞异常", "竞态条件", ...])

    返回:
        precision, recall, f1
    """
    # TP (True Positive): Agent 找到的 bug 在标注里 → 真阳性
    tp = sum(1 for p in predictions if p in ground_truth)
    # FP (False Positive): Agent 找到的 bug 不在标注里 → 误报
    fp = sum(1 for p in predictions if p not in ground_truth)
    # FN (False Negative): 标注的 bug 没被找到 → 漏报
    fn = sum(1 for t in ground_truth if t not in predictions)

    precision = tp / (tp + fp) if (tp + fp) > 0 else 0  # 找的对不对？
    recall = tp / (tp + fn) if (tp + fn) > 0 else 0     # 找的全不全？
    f1 = 2 * precision * recall / (precision + recall) if (precision + recall) > 0 else 0  # 综合评分

    return {"precision": precision, "recall": recall, "f1": f1}
```

**实测结果**：

```
文件: sandbox.py       → 2/3 bugs found
文件: agent.py         → 2/2 bugs found
文件: streaming.py     → 1/1 bugs found
文件: telemetry.py     → 0/1 bug found
文件: config.py        → 0/1 bug found
文件: llm_client.py    → 0/1 bug found
─────────────────────────────────
总计: 6/10 = 60% recall, 100% precision
```

**面试话术**：*"评估是 Agent 项目最容易忽视的部分。没有评估，你就不知道这次改 prompt 是变好了还是变差了。我的 eval set 覆盖 6 个文件 10 个真实 bug，每次改完代码跑一遍就知道回归没回归。"*

---

### 30. 这个项目和工业级系统的差距在哪里？怎么改进？

**问题**：你的项目距离生产环境还差什么？

**回答**：

| 差距 | 当前实现 | 工业级方案 | 改进优先级 |
|------|---------|-----------|-----------|
| **用户管理** | JSON 文件持久化 | PostgreSQL + 用户组 RBAC | ⭐⭐⭐ |
| **Token 管理** | 内存吊销列表 | Redis Set + TTL 自动过期 | ⭐⭐⭐ |
| **向量搜索** | SQLite FTS5（全文搜索） | ChromaDB/Pinecone 语义搜索 | ⭐⭐ |
| **容器化** | 单机 Docker | K8s + HPA 自动扩缩 | ⭐⭐⭐ |
| **监控告警** | Prometheus 指标 | Grafana 面板 + PagerDuty 告警 | ⭐⭐ |
| **日志** | JSON Lines 文件 | ELK/Loki 集中式日志 | ⭐⭐ |
| **API 版本** | 无版本控制 | /v1/ /v2/ 多版本并行 | ⭐ |
| **单元测试** | 12 个集成测试 | 80%+ 覆盖率 + 快照测试 | ⭐⭐⭐ |
| **缓存** | 单进程 LRU | Redis 分布式缓存 | ⭐⭐ |
| **并发** | 单线程处理 | 异步 + 队列 + Worker 池 | ⭐⭐⭐ |

**最小改进路径**：

```
第一步: PostgreSQL + Redis（核心短板）
   ↓
第二步: K8s 部署（可扩展性）
   ↓
第三步: 80% 测试覆盖（质量保障）
   ↓
第四步: ELK 日志 + Grafana 面板（可观测性）
```

**面试话术**：*"承认差距比掩饰更有价值。这个问题面试官想听的不是你的项目有多完美，而是你有没有思考过'如果用户量翻 100 倍怎么办？'——我的回答是先把用户存储和缓存换成 PostgreSQL + Redis，这是扩容的基础。"*

---

 > 📝 **使用建议**：
> - 先按目录挑 3-5 个高频问题自测
> - 红色标记不熟悉的问题重点复习
> - 面试答完追问："我这里刚好有个类似的实践……"——用项目经验加分
> - 每次面试后回来标记被问到的题目，更新自己的题库

---

*最后更新：2026-07-23 | 基于 code-review-agent 项目实战*
