# 差异分析：claude code harness.md vs 本项目（code-review-agent）

> 对比日期：2026-08-13
> 参照文档：`claude code harness.md`（Claude Code Harness 架构伪代码，七大块）— 项目内与 `新准备/Agent面试/` 下两份内容相同
> 对比对象：本项目 `src/` 全部核心模块（harness / tools / multi_agent / llm_client / api / app）

---

## 一、总览：文档七大部分 vs 项目对应实现

| 文档部分 | 文档职责 | 项目对应文件 | 实现度 |
|---|---|---|---|
| ① 数据契约层 | Message / Tool / QueryParams / LoopState / QueryEvent | [agent.py](src/harness/agent.py) ToolCall/ToolDefinition；LangGraph AgentState（TypedDict + reducer）；事件 = dict 流 | ✅ 等价（形态不同） |
| ② 工具加载 | load_mcp_tools（MCP 协议连远端工具） | [cli.py](src/app/cli.py)、[server.py](src/api/server.py)、streamlit_app 静态注册 ToolDefinition | ❌ 无 MCP，工具静态注册 |
| ③ 上下文压缩 | perform_auto_compact（4 层：裁剪大 tool_result → microcompact → LLM 摘要 → 熔断），**token 估算驱动** | [memory.py](src/harness/memory.py) ContextMemory（sliding/summary/hybrid），**消息条数驱动** | ⚠️ 同理念、简化版 |
| ④ 模型 API 适配 | call_model_api：流式解析 text_delta / tool_use_start/delta/stop，input_schema **执行前校验** | [llm_client.py](src/llm_client.py) + [streaming.py](src/harness/streaming.py) 状态机；DeepSeek XML 兼容；prompt caching | ⚠️ 流式解析✅，**参数校验❌** |
| ⑤ 工具执行与权限 | check_permissions（allow/deny/**ask**）→ 沙箱 → 结果 is_error | [auth.py](src/harness/auth.py) HITL 三级风险 + [sandbox.py](src/harness/sandbox.py) 进程级/Docker 沙箱 | ✅ 更强（见差异 2） |
| ⑥ 核心循环 | query_loop：预压缩 → 模型 → **并行执行工具** → 更新状态 → max_turns | [agent.py](src/harness/agent.py) run()/run_streaming() 循环 | ⚠️ 循环✅，**工具串行** |
| ⑦ Multi-Agent 委派 | execute_sub_agent：sync / **background / remote** 三种模式，上下文隔离 | [langgraph_orchestrator.py](src/multi_agent/langgraph_orchestrator.py)：plan → Send 并行 → review → fix → verify | ✅ 更强（图编排） |
| ⑧ 入口与 REPL | main()：初始化 + REPL 长驻 + 斜杠命令（/help /compact /exit） | [cli.py](src/app/cli.py)（单任务）+ [streamlit_app.py](src/app/streamlit_app.py)（Web UI）+ [server.py](src/api/server.py)（FastAPI + JWT） | ✅ 产品化升级，无 REPL |

---

## 二、逐项对比

### 相同点（概念一一对应）

| # | 文档概念 | 项目实现 | 位置 |
|---|---|---|---|
| 1 | `Message`（role/content/tool_use_id） | 内部 dict 消息 + LangChain BaseMessage（HumanMessage/AIMessage） | [langgraph_orchestrator.py:35](src/multi_agent/langgraph_orchestrator.py#L35) |
| 2 | `Tool`（name/input_schema/call） | `ToolDefinition`（name/description/parameters/fn） | [agent.py:13-18](src/harness/agent.py#L13-L18) |
| 3 | `LoopState`（messages/turn_count/compact_tracking） | AgentHarness 实例属性（messages/turns_taken）+ LangGraph `AgentState` | [agent.py:27](src/harness/agent.py#L27) |
| 4 | `QueryEvent`（text_delta/tool_start/tool_result/error） | 事件 dict：`text_chunk` / `tool_call_detected` / `tools_executed` / `finished` | [agent.py:51-71](src/harness/agent.py#L51-L71) |
| 5 | 流式 tool_use 解析（start→delta 缓冲→stop 出块） | StreamingParser 状态机（IDLE / TOOL_OPEN / TEXT_OPEN），partial_json 累积后 json.loads | [streaming.py](src/harness/streaming.py) |
| 6 | 权限 allow/deny/ask 三态 | HITL：SAFE / MODERATE / DANGEROUS 三级 + run_command 危险子串扫描 | [auth.py:11-33](src/harness/auth.py#L11-L33) |
| 7 | 沙箱隔离后执行 | Sandbox（进程级 subprocess+线程+临时目录）+ DockerSandbox（9 项容器隔离） | [sandbox.py](src/harness/sandbox.py) |
| 8 | 拒绝不崩 Agent，返回 is_error 结果 | 工具异常 → `Tool error: {type}: {e}` 字符串结果回传，循环继续 | [agent.py:150-155](src/harness/agent.py#L150-L155) |
| 9 | max_turns 防死循环 | max_turns + `_force_finish()` 收尾 | [agent.py:161-169](src/harness/agent.py#L161-L169) |
| 10 | 每轮模型调用前压缩 | `_pre_turn()`：should_compact → compact | [agent.py:73-79](src/harness/agent.py#L73-L79) |

### 差异点 A：项目有、文档没有（项目的增强）

| # | 增强 | 说明 | 位置 |
|---|---|---|---|
| A1 | **write_file 截断守卫（零副作用拒绝）** | 结果 < 原文一半 → REFUSED，不备份不写 tmp 不改文件 | [git_tools.py:193-197](src/tools/git_tools.py#L193-L197) |
| A2 | **写前自动备份 + verify_fix 自动回滚** | 首次修改生成 `.bak`；完整性检查（ast.parse + 尺寸比 + 符号缺失 ≥30%）失败 → restore_from_backup | [git_tools.py:199-279](src/tools/git_tools.py#L199-L279)、[langgraph_orchestrator.py:235-317](src/multi_agent/langgraph_orchestrator.py#L235-L317) |
| A3 | **NOT_APPLIED 假成功检测** | 声称 FIXED 但无 .bak（未落盘）→ 降级 NOT_APPLIED | [langgraph_orchestrator.py:253-286](src/multi_agent/langgraph_orchestrator.py#L253-L286) |
| A4 | **LangGraph 图编排 + Send 并行分片** | plan → 条件边 → `Send("execute_one", {finding})` 每个 finding 独立并行 → review → fix 按文件分组（同文件串行防覆盖）→ verify_fix | [langgraph_orchestrator.py:86-109](src/multi_agent/langgraph_orchestrator.py#L86-L109) |
| A5 | **按角色过滤工具** | `_make_agent` 只注入该角色允许的工具（Planner 3 个 / Executor 2 个 / Fixer 2 个）— 最小权限 | [langgraph_orchestrator.py:65-72](src/multi_agent/langgraph_orchestrator.py#L65-L72) |
| A6 | 4 个角色独立 system prompt + 结构化文本协议 | FINDING\| / VERDICT\| / FIXED\| 行协议，`|` 分隔，解析容错（markdown 前缀/冒号变体） | [agents.py](src/multi_agent/agents.py)、[langgraph_orchestrator.py:320-386](src/multi_agent/langgraph_orchestrator.py#L320-L386) |
| A7 | prompt caching（cache_control ephemeral） | system 置缓存断点 + 最后一个 tool 定义置断点；并行分片共享前缀 → 真收益 | [llm_client.py:111-125](src/llm_client.py#L111-L125) |
| A8 | LLMCache（LRU + TTL，线程安全） | 无工具调用时命中缓存，可省 50%+ token | [llm_cache.py](src/harness/llm_cache.py) |
| A9 | ModelRouter 任务→模型分级 | 强任务 deepseek-v4-pro[1m] / 简单任务 flash | [model_router.py](src/model_router.py) |
| A10 | Telemetry（JSON Lines 结构化日志 + node_stats 节点级统计） | turn_start/end、tool_call 耗时、error、usage；LangGraph 每节点 turns/tokens/elapsed | [telemetry.py](src/harness/telemetry.py)、[langgraph_orchestrator.py:74-83](src/multi_agent/langgraph_orchestrator.py#L74-L83) |
| A11 | **产品层**：JWT 认证（bcrypt + access/refresh + 轮换 + 吊销列表）、限流、CORS 白名单、Prometheus 指标、Dashboard | 文档完全没有 | [server.py](src/api/server.py) |
| A12 | SQLite 会话/消息持久化 + FTS5 语义搜索 | 零依赖全文检索（升级路径 ChromaDB，API 不变） | [database.py](src/storage/database.py)、[vector_store.py](src/memory/vector_store.py) |
| A13 | **评测体系**：eval_dataset（50+ 标注样本）+ 42 个测试 + CI 双门 | recall 44.4% / precision 98.0% / F1 0.61 | tests/ |
| A14 | DeepSeek XML 工具调用兼容（`<invoke><parameter>`） | 模型输出文本标签时兜底解析 | [llm_client.py:22-42](src/llm_client.py#L22-L42) |
| A15 | `_force_finish` 优雅收尾 | 超轮数后注入"给最佳答案"提示再调一次模型（文档只报错退出） | [agent.py:161-169](src/harness/agent.py#L161-L169) |

### 差异点 B：文档有、项目没有（差距项，面试可讲"生产演进方向"）

| # | 文档特性 | 项目现状 | 影响 / 面试价值 |
|---|---|---|---|
| B1 | **MCP 工具集成** | 工具静态注册，无外部工具协议 | 工具生态封闭；可扩展但无标准协议。**面试价值高**：讲"接入 MCP 后工具来源透明化" |
| B2 | **Token 估算驱动的自动压缩** | 按消息条数（window_size+4）启发式触发，不看 token | 长 tool_result 会撑爆上下文；无"裁剪超大工具结果"层、无熔断 |
| B3 | **input_schema 执行前参数校验** | schema 只传给模型展示，执行前不校验 | 非法参数直接进 fn，靠 try/except 兜底；无 SchemaValidationError→重试机制 |
| B4 | **工具并行执行**（StreamingToolExecutor） | 工具严格串行（一条消息等一个结果） | 多工具场景（如一次读 5 个文件）延迟累加 |
| B5 | **真正的阻塞式 ask（人工确认）** | HITL 默认 `approved = (risk == MODERATE)` 自动批准；只有传 callback 才真询问 | "Human-in-the-loop"名不副实：写操作默认放行 |
| B6 | **取消信号贯穿（AbortController）** | 无任务取消机制（只能等循环自然结束） | 长任务无法中断，用户体验差 |
| B7 | **background / remote 子任务模式** | 只有同步委派（LangGraph 图内） | 无法"后台跑子任务 + 轮询" |
| B8 | **命令生命周期审计（command_uuid）** | 无命令级跟踪 | 审计粒度到工具名，不到"某次具体命令实例" |
| B9 | **REPL + 斜杠命令**（/compact /help /exit） | CLI 单任务执行即退，无交互壳 | 产品化替代了 REPL，可接受；/compact 手动触发缺失 |
| B10 | tool 结果统一转 **tool_result block**（map_result_to_block + is_error 标志） | 工具返回原始字符串，错误靠文本前缀 | 结构化程度低，前端无法区分"正常空结果"与"失败" |

---

## 三、本项目优点（面试陈述用）

1. **安全纵深远超参照文档**：截断守卫（零副作用拒绝）→ 写前备份 → 三重完整性校验（语法/尺寸/符号）→ 自动回滚 → 假成功降级。文档里"工具失败不崩"是一层，本项目是**五层修复安全网**——这是真实事故驱动设计的（llm_client.py / jwt_auth.py 两次截断事故），面试讲起来最有说服力。
2. **真正的并行多 Agent 编排**：文档只有"父→子委派"，项目用 LangGraph `Send` API 做 finding 级并行分片 + 按文件分组防写冲突——`operator.add` reducer 解决并行状态合并，是 LangGraph 正确用法。
3. **最小权限工具注入**：每个角色只拿到自己的工作工具（Planner 无 write_file），配合 HITL 形成"角色权限 + 风险分级"双层防线。
4. **产品化完整**：JWT 认证（轮换+吊销）、限流、CORS、Prometheus、Dashboard、SQLite+FTS5——从"能跑的命令行"到"能部署的 Web 服务"。
5. **可观测性**：JSON Lines 结构化日志 + 节点级 node_stats，与 LangGraph 图自然结合。
6. **成本工程**：prompt caching + LLMCache + ModelRouter 三件套，是面试"降本"题的标准答案组合。
7. **评测闭环**：50+ 标注样本 + 42 个测试 + 真实 eval 数据（precision 98%），证明"不是玩具"。

## 四、可改进点（按优先级）

| 优先级 | 改进 | 做法 | 难度 |
|---|---|---|---|
| P0 | **执行前参数校验**（B3） | `jsonref`/手写 validate against `parameters` schema，失败→错误回传模型重试 | 低（半天） |
| P0 | **HITL 真阻塞**（B5） | Streamlit/CLI 场景注册 approval_callback 弹确认框；后端场景保持自动（无界面） | 低 |
| P1 | **token 预算压缩**（B2） | 先加"裁剪超大 tool_result"层（文档第一层），再估算 token 触发摘要 | 中 |
| P1 | **取消机制**（B6） | run_streaming 里 yield 处检查 cancel flag；CLI Ctrl+C 传播 | 中 |
| P2 | **并行工具执行**（B4） | 同轮多 tool_call 用 asyncio.gather（工具只读时可安全并行） | 中 |
| P2 | **MCP 接入**（B1） | 项目本身就有 SSE/HTTP 能力可讲；接一个最小 MCP server 演示 | 中 |
| P3 | **is_error 结构化**（B10） | 工具返回 (content, is_error) 元组，日志/前端可区分 | 低 |
| P3 | **/compact 手动命令** | CLI 加 `--compact` 或交互模式 | 低 |

**改动与面试的权衡**：P0 两项改动最小、面试最容易被追问（"参数校验怎么做的？""HITL 用户怎么确认？"），优先做；P1 的 token 压缩第二层（LLM 摘要）项目已有 summary 策略，补第一层即可，也是高频面试题。

---

## 五、一句话总结

> **本项目 = 参照文档的完整 harness 骨架（循环/流式/权限/沙箱/压缩）+ 三层增强**：
> ① 安全增强（修复五层防线，文档没有）、② 编排增强（LangGraph 并行图，文档只有串行委派）、③ 产品增强（JWT/存储/评测/可观测性，文档没有）。
> 差距集中在 ④ 协议层（MCP / 参数校验 / 结构化结果）与 ⑤ 运行时体验（取消 / 并行工具 / 真 HITL）——恰好是面试官最喜欢追问的"生产级 harness 还缺什么"。
